{ change name / identifier in makeprogram token }
program User(ouput); 

{ keep all declarations }
Uses sysutils;  

var i, f, written : integer; msg: string; 

begin
    i := 1; 

    { send signal to priv }

    { send value of i }

    { wait for signal from priv }

    { recieve signal }
end.