program User(param); 
{ must have output for now }

var i, r, output : integer;

begin
    i := 1; 

    { send i to priv }

    { wait for answer }
 
    r := { unpriv answer from priv }

    writeln(r);
end.

(*

Create input / output variables

*)