# Build

>$ build.sh

# Run

>$ run.sh <input .pas>

# Example

>$ bash build.sh

>$ bash run.sh ../examples/simple.pas

## Lexer

>$ flex ./lexer.l

>$ make lexer

## Parser Only

>$ bash ./build.sh

Provide an input .pas file (optional)

>$ ./parse < <file.pas>